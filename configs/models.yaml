models:
  gpt-5.4:
    provider: gpt
    base_url_env: GPT_BASE_URL
    api_key_env: GPT_API_KEY
    endpoint: responses
  glm-5:
    provider: glm
    base_url_env: GLM_BASE_URL
    api_key_env: GLM_API_KEY
    endpoint: chat_completions

aliases:
  proposer_default: gpt-5.4
  skeptic_default: glm-5
  moderator_default: gpt-5.4
