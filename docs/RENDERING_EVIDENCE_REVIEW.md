# Rendering 验证：来源 ID 拒绝审查

本次暂停是正确的协议拒绝。结构修复调用把一个原本正确的来源 ID 少写了一个字符；错误位于检索轨迹复写列表，不在 19 条 finding 的来源或摘录中。本次检查没有发现来源注册器丢失该记录的证据。

这是开发代理对已保存产物的机械审查，不是科学专家裁决。检查使用 g203 隔离目录内 SQLite `mode=ro` 和实际保存的工具返回；没有调用模型或 MCP，没有修改原始响应、任务、卡片、来源或账本，也没有注册部分 findings。

## 冻结对象

- Run：`arc-vnext-validation-20260907.ARC_RENDERING_VALIDATION.discover`
- Task：该 run 的 `.shared_investigation`
- Task 状态：`PAUSED_PROTOCOL`，错误 `unknown_source_id`，`accepted_result=null`。
- 主线程提供的停止费用：1.303072–1.303085 CNY，66 次账本调用；本次未重算费用。
- Artifact 根：`/home/g203/zhanghaonan/adversarial-research-copilot/.arc-validation/artifacts/`
- Task artifact 目录：`runs/arc-vnext-validation-20260907.ARC_RENDERING_VALIDATION.discover/tasks/805c7f22e9760ba8422b8da3052a3b42eac47ed7eb5f3d32959f12ed60a99292/`
- 最终冻结状态：上述目录下 `states/f646be8120474ba0b3e8d90afd7a22fc.json`，SHA256 `b948a0b6f2cba11bc33c124cb5123ac301a9f79078cc6066889c74e326f9aa98`。

## 具体拒绝字段

候选输出的零基路径 `$.result.actual_searches[1].source_ids[5]` 写成：

`src_d981b8788f84791b2af69a2f07ae529`

实际工具 `tool_a18a8794e6dc476a836949b39cd63b2d` 的 `source_ids[5]` 和 `result.sources[5].source_id` 都是：

`src_d981b87884f84791b2af69a2f07ae529`

后者存在于来源表。它对应 T-MOTOR 网页 `https://uav-cn.tmotor.com/`，是 T-LESS 查询中的无关命中。记录为 `content_origin=metadata`、`access_status=metadata_only`、`content_path=null`；该任务没有对它调用 `read_record`。因此，这不是“已读论文正文对应 ID 被注册器替换”，也不是正文引用列表里泄漏了一个未注册 ID，而是模型复写搜索结果 ID 时漏掉一个 `4`。

18 条 `actual_searches` 与实际搜索工具的 trace ID、operation、query、完整有序 source ID 列表逐项比较，只有索引 1 的这一项不同。全输出有 157 次结构化来源 ID 出现、136 个不同字符串；只有该错误字符串不在来源表且不在本任务工具返回的可见来源集合。其余来源引用都已注册并由本任务工具返回。

## 结构修复确实引入了 ID 错误

原始生成调用 `call_5d438dbd7de24eaf9fb92f38d6173a6b` 的公开 `message.content` 保存于同目录的 `states/62d22fcd4f7241a59ae422b526bf7df8.json`，SHA256 `708bd0ff6b40af33ccd9e22c570e79e41ab309e1b60263cd3a3ae5b8babeb393`。

该响应 `finish_reason=stop`、`repair_count=0`，JSON 不合法。完整 JSON 对象结束后还有 315 个字符，以外置的 `,"note":` 开始。保存的校验错误为 `json_invalid: trailing characters at line 1 column 50697`；Python 的字符下标与此诊断列号不是同一个计数单位。只为审查而读取首个 JSON 对象，不代表允许运行时截尾接受它。

原始内容中正确 ID 出现一次，错误 ID 没有出现。随后仅有一次结构修复：`call_2600f86e41dd415d939aa609d75f6af4`，`finish_reason=stop`、`repair_count=1`。修复后 JSON 可解析，但正确 ID 消失，错误 ID 出现一次。修复快照为同 task 目录的 `repair.json`。

对首个原始 JSON 对象和修复后对象做字段比较，还能看到 `note` 归位、19 条 finding 的 `conditions` 类型修正，以及 capability request 字段调整。因此不能把原始对象说成“除了尾部就已满足全部 schema”。来源 ID 的单字符变化仍是独立且明确的修复损坏。最终运行时在语义引用校验处停止，没有接受修复后的对象，也没有继续第二次修复。

## Finding 与正文机械核查

- 最终候选含 19 条 findings，涉及 13 个不同来源；全部来源已注册且在本任务实际工具返回中可见。
- 19 条 `excerpt` 均在其各自已保存的来源正文中精确出现一次；19 条也都能在本任务至少一条实际返回给模型的正文片段中精确找到。不是只对模型尚未读取的缓存余文做匹配。
- 完整工具轨迹有 46 条：18 次 `search_web`、20 次 `read_web`、5 次 `read_record`、3 次 `read_paper`。工具条数与主线程给出的 66 次模型及工具账本调用属于不同口径。

这些检查只能说明引用对象与摘录可追溯，不能证明每条摘录足以支持 finding 的全部科学、许可或可行性主张。此次审查没有展开科学范围判定，也没有据此给发现“通过”结论。19 条摘录通过机械检查不改变整个候选仍被协议拒绝的事实。

## 判断边界

现有证据支持保留 `unknown_source_id` 拒绝：程序不应猜测或自动更正来源 ID。本次失败暴露的是结构修复输出没有保持引用标识符，而不是已证实的来源注册或可见性校验缺陷。没有人工补 ID、接受候选、注册部分 findings，或向后续模型注入本审查。

## 有限科学内容复核：四个关键主张

以下追加审查仍使用同一个被拒绝的冻结候选。F3、F8、F14、F15 指 `result.findings` 的零基下标；候选中的 `claim_id` 均为 null，没有注册为正式 claim。定位采用实际保存 UTF-8 文本的零基字符区间 `[start,end)` 与一基行号，不指定未经核实的 PDF 页码。这里区分原文报告、模型设计推断和未取得材料，不把开发代理判断当成专家真值。

### F3：T-LESS 的几何困难成立，独立归因尚无证据

候选使用 `relation=supports`，从对象无明显纹理、形状相似与对称，推到“几何轴误差可被独立于纹理外观触发”和适合作为几何轴干预对象。

来源 `src_8811a98f8a964341a428584b93c0b547`，保存 20000/46429 字符。正文文件为 `sources/src_8811a98f8a964341a428584b93c0b547/472d03ca7d182c9fac464ef0716fd2c1fa345590a0a5cb0145eca68dcf76055e.txt`，文件名即已登记 SHA256。

- **原文支持的事实：**摘要 `chars:213:1682` / L3 描述 30 个工业相关对象、相似与对称、互为部件，以及 20 个复杂度不同的测试布局。它支持几何困难和数据集选择动机。
- **原文给出的条件：**引言 `chars:2316:3041` / L6 明确表观由全局形状、颜色、反射和光源配置共同决定；方法还利用强度边缘与深度。`chars:4097:5050` / L8 描述从视球系统采样图像。20 个布局固定不等于相机视角、物体相对相机的姿态或像素表观固定。
- **超出原文的部分与影响：**这些描述没有进行外观变量受控下的几何错误因果实验，也没有证明可独立触发某一类错误。把完整复合 claim 标成 `supports` 过强；适合作为候选锚点是设计判断。后续对照若把它当成已经确立的独立几何轴，就会把原本需要检验的前提当成已知条件。

### F8：默认多因素随机化有据，“不可分离”需要限定

候选使用 `relation=motivates`，描述 BlenderProc4BOP 同时随机化材质、灯光、物体姿态与相机，并称默认两轴不可分离。

来源 `src_7cbdfa883d3e4e19b7dc2f59e2cf6d60`，保存完整网页 4333 字符，正文文件 `sources/src_7cbdfa883d3e4e19b7dc2f59e2cf6d60/fa44656864408ba9f4175db5457c027cea2b3278bccbbc0ba6f9545e713f5312.txt`。

- **直接支持：**`chars:1878:2398` / L9 描述随机墙材质、灯光强度与颜色、点光源位置；`chars:2399:2889` / L10 描述随机物体材质与 PyBullet 落体造成不同遮挡。默认生成流程同时改变多个因素，因此仅比较默认整套生成流程不能识别各因素贡献。这一研究动机与 `motivates` 相符。
- **姿态分布的准确范围：**`chars:2890:3145` / L11 是让相机覆盖测试 GT 的方位、仰角、距离范围，不是逐图复制 GT 姿态，也没有承诺匹配完整概率分布。L10 还明确 LM 的直立摆放例外。`chars:3466:3585` / L16 单列 `bop_scene_replication` 示例，说明复制测试布局是另一个具名操作，不能视为默认流程已经执行。
- **需要限定的推断：**多个参数都被随机化，不证明两个参数族统计上无法独立采样，也不证明受控消融不可识别。若“不可分离”仅指当前默认结果缺少单因素对照，可以成立；若指算法或物理上不可能分开，则没有得到这段文档支持。影响是应保留“需要设计控制”的问题，而不能据此预设研究必然发现不可分解耦合。

### F14：PNDR 提供光照域移前例，不能直接当作训练单轴消融

候选使用 `relation=supports`，把 HB Scene 5/10 的内容恒定、光照变化与三行 ADD AUC 数值，作为固定场景外观单轴实验的前例。

来源 `src_5461ae3912e9438cb88a8bfcc83255c6`，保存 22000/58496 字符，正文文件 `sources/src_5461ae3912e9438cb88a8bfcc83255c6/22adfb3afbe9319018472ee802c629502ef6a5a69f0b56dc79a4d33ed6255a81.txt`。

- **数值和作者目标确有依据：**§6.1 的表头区分 Scene 5 与 Scene 10 (Lighting)。Real 行 `chars:20339:20446` / L60 的 Scene 10 mean 为 22.24；RayTraced-4352 行 `chars:21172:21292` / L67 为 37.35；Ours-1088 行 `chars:21293:21408` / L68 为 40.44。§5.1 说明使用多阈值 ADD 的 AUC，而不是单阈值检测 AP。`chars:21409:22000` / L69 明确两场景具有相同对象与环境，拍摄光照显著不同，作者目标是隔离这种光照域移。
- **训练归因仍不足：**表中比较了 Real、RayTraced、Ours 等不同训练方式，且选取的 RayTraced-4352 与 Ours-1088 样本数量不同。光照变化的测试条件，不等于训练实验只改光照且其他随机化、样本量与方法均相同。该证据支持模型在指定光照域移上的表现比较，不能直接证明材质/光照相对于几何的独立贡献。
- **真实读取缺口：**缓存 L69 正好截在 `We spl`，训练/测试划分说明未完整取得。保存部分不能核完整拆分、配对规则或逐帧姿态是否相同。候选已注明缓存上限，但其支持说明仍不宜把完整可复现协议视为已经核实。该结果也不能直接外推为工业固定工位结论。把它选作最接近前例是本次有限检索中的相对判断，不是对全部文献的证明。

### F15：制造场景结果有支持，轴比较与固定场景表述超出已读范围

候选使用 `relation=supports`，从作者的重要因素名单推到关键轴依赖场景、因素组消融而非外观/几何正交，并称其证明固定工业场景 DR 的高 mAP。

来源 `src_51daccf6f95c4821aff1b749bcff1382`，保存 18000/38331 字符，正文文件 `sources/src_51daccf6f95c4821aff1b749bcff1382/223c1e52e51a626fca7c71a80d7ce2c9a2c15d7d6ec95d682d6669f8d640f865.txt`。

- **支持的事实及数值修正：**摘要 `chars:125:1419` / L3 确实列出材质、渲染方式、后处理和干扰物为重要因素。它报告机器人数据集 96.4%，SIP15-OD 三个用例分别为 94.1%、99.5%、95.3%。所以候选不加数据集限定的“mAP@50 最高 96.4%”并不准确，同段已经给出 99.5%。这些是作者在其协议下报告的检测 mAP，不能单凭高分归因于某一个渲染轴。
- **遗漏的实验条件：**`chars:14395:15050` / L40 明确 YOLOv8 使用 COCO 预训练，再在合成训练/验证图上训练。“synthetic-only”可描述目标任务训练数据来源，但不能扩成从未利用真实图像预训练。`chars:16390:17282` / L44 明确 321 张真实图来自传送带、蓝色送货箱、工位/平面等不同情境，并使用两种相机；它不能直接证明单一固定场景下的效果。
- **未读消融与负向结论：**`chars:10846:11365` / L30 给出五组参数和组合管线，但实验结果仅在 `chars:17457` / L50 开始，缓存末端仍在通用训练配置说明，未取得消融表。摘要的重要因素名单没有光照，不等于光照无效；参数分组描述也不能证明作者没有正交比较。因此“关键轴高度依赖场景”可作为跨文献设计推断，而“本文消融非两轴正交”在当前保存材料下仍需核原文实验。这一点直接影响候选是否有足够新意：不能用未读到消融代替不存在相近实验的证据。

### 此次追加复核的范围

四条均有可追溯的相关原文；主要缺陷是复合主张把数据集属性、作者报告、实验设计推断和因果归因放在同一个关系标签下。F8 的动机关系较贴近材料，F3/F14/F15 则各有需要收窄或保留待核的部分。本次不修改它们，不据此替研究流程作入选或否决决定，也不把审查结果注入后续模型。其余 15 条 finding 的科学范围没有在这次有限抽查中审完。
