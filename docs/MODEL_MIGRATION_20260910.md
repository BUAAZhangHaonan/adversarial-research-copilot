# 2026-09-10：当前模型统一为 DeepSeek V4.1 Flash

本次只迁移当前调用配置与计价。历史科研结果、逐调用模型标识、价格快照和累计费用不改写，也不据此声称抽卡质量已经改善。

## 当前模型和官方依据

- ARC 配置别名：`deepseek-v4.1-flash`。
- 实际 API 请求模型：`deepseek-flash`；官方对应版本为 `DeepSeek-V4.1-Flash`。
- 所有新角色默认使用该唯一别名，包括 scout、ideator、editor 及保留的旧角色入口。支持用户明确配置兼容协议别名的原有能力不变。
- 保持思考模式 enabled、reasoning effort max、1,000,000 token 上下文计价边界和 384,000 token 输出上限。没有通过降低思考强度或截断生成节省费用。

依据为 2026-09-10 当日直接读取的[官方模型与价格页](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)和[官方更新日志](https://api-docs.deepseek.com/zh-cn/updates/)。更新日志明确旧 V4 Flash 已下线、旧名称暂时转发到 V4.1 Flash；因此当前调用使用新 API 名字，不沿用容易误认版本的旧名字。V4 Pro 的后续退役安排不影响本项目现在主动停止选择它。

免费 `/models` 核查在 `2026-09-10T09:31:12Z` 返回 HTTP 200，列出 `deepseek-flash` 和 `deepseek-v4-pro`。该结果证明账户可见模型目录，不是生成质量或真实生成验收；本次核查没有调用 chat completions。脱敏记录位于 `work/model-migration-20260910/models-api-check.json`。当前私有 `.env` 中没有需要迁移的 ARC/DeepSeek 模型选择变量，凭据未输出、未提交；其他服务变量未改。

## 当前人民币计价

下表单位为每百万 token；高峰仍按北京时间周一至周五 09:00–12:00、14:00–18:00。

| 时段 | 缓存命中输入 | 未命中输入 | 输出 |
| --- | ---: | ---: | ---: |
| 空闲 | 0.02 元 | 1.00 元 | 4.00 元 |
| 高峰 | 0.04 元 | 2.00 元 | 8.00 元 |

当前价格快照 `configs/pricing.json` 仅含实际模型 `deepseek-flash`。沿用原有完整上下文与输出联合上界，每次请求保守预留上界为 **4.304 元**；这是最坏情况的准入边界，不是一次任务预计必然花费。实际结算仍使用真实 usage，缓存未知与跨时段请求保留合理费用区间，reasoning tokens 不重复计费。

原 `deepseek-cny-2026-09-07` 内容完整保留在 `configs/pricing-2026-09-07.json`，历史每次请求自带的价格快照也保持原样。旧 V4 回放与计价单测明确固定到历史模型和历史快照。当前快照拒绝为旧模型名字发起新购买，错误包含模型名与价格快照 ID；不会把旧暂停任务悄悄改绑为新模型。

旧对照辅助代码中不再硬编码 Pro/Flash 分工，统一尊重 `settings.research_model`。它们的历史报告不变，本次不开展新的模型排行榜或 A–E 付费对照。

## 验证边界

新增离线验收核对：默认别名与实际 API 名字、全角色选择、完整输出与思考参数、峰谷边界精确计价、未知 usage 上界、历史快照不变、真实 SDK 请求序列化、账本结算与缓存恢复、旧模型在新价格下付费前拒绝。

本轮完整回归833项通过；随后两项真实定向任务共9次请求，requested/returned均为 `deepseek-flash`，已结算0.736622–0.736630元。软件与研究边界、逐次计价见[本轮交付](CURRENT_UNDERSTANDING_20260910.md)。免费目录查询与mock SDK检查仍只按各自范围解释。
