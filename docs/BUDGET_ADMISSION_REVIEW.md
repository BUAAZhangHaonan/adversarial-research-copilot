# 真实预算暂停与输入计数边界

本次 `explicit_input.develop` 已花人民币 4.106986–4.107017 元，阶段额度20元，剩余15.892983元。round1的proposer和skeptic已保存；moderator未完成，下一请求因预算预留不足暂停。没有科研assessment，不能将它称为科学否决或完整入口通过。

当前Pro请求的保守预留是15.912元：按已核查的峰时每百万输入9元、输出27元，联合上下文上限1000000、完整输出上限384000，最大值为 `(1000000 × 9 + 384000 × (27 − 9)) / 1000000`。Flash对应5.304元。预留不是实际收费；返回完整usage后按实际输入、缓存及输出结算。实现见 `src/arc/pricing.py` 的 `admission_bound`，价格快照见 `configs/pricing.json`。

这个默认组合会在Pro阶段实际花费略超过4.088元后，无法准入下一次完整请求，即使账面余额还很多。这是严格上界的可用性限制，不能靠把余额称作耗尽、缩短输出或重置账户掩盖。

为核查能否建立更紧的可靠输入上界，额外只读检查了官方资料：

- [Token Usage](https://api-docs.deepseek.com/quick_start/token_usage/) 链接当前V4 tokenizer。ByteLevel+BPE对完整、确定的输入字符串可精确计数，UTF-8字节数也可给该字符串的保守上界。
- [官方V4编码器](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro/blob/main/encoding/encoding_dsv4.py) 描述max、工具、reasoning历史及格式指令的序列化，但接口结构与托管HTTP请求不同。

尚未找到托管API完整前处理与该开源编码器一致的保证，也未找到公开精确输入计数接口。因此，HTTP JSON字节数或本地开源序列化后的token数，不能直接宣称为托管计费输入的已证上界。本次保持原有预留，没有以启发式缩减来跨过0.019017元差额；核查本身未发付费模型请求。

依据执行任务书§7.3，只有用户明确增加阶段预算后才能追加授权记录，并从同一run恢复；100元父账户仍不可突破。本次未增加额度。已保存任务、响应、工具检查点和已花费用保留，`resume`本身不会发放新的20元。
