# ARC 历史目录归并记录

2026-09-12，按用户要求将 `/home/g203/zhanghaonan/arc-vnext-20260907` 归并到唯一主仓库 `/home/g203/zhanghaonan/adversarial-research-copilot`，随后删除旧目录。

## 目录性质与归并结果

旧目录属于 ARC。它此前已解除 Git worktree 登记，没有独立分支或剩余源代码；保留的是历史开发材料、付费调用原件及研究数据库。最近的多模态幻觉任务也使用这里的数据。

| 内容 | 当前主仓库内位置 | 处理 |
|---|---|---|
| 主要研究档案 | `.arc-validation/` | 同一文件系统内直接移动，保留约 33 GiB 数据 |
| 独立费用验证档案 | `.arc-validation-unmetered-20260911/` | 独立保留，不并账 |
| 历史脚本和验证日志 | `work/legacy-20260907/` | 保留 158 份原有文件 |
| 不同版本及独有文档 | `docs/history/20260907/` | 保留 10 份旧版本和 9 份历史计划，不覆盖当前文档 |
| 相同报告、文档和任务书 | 主仓库既有对应路径 | 逐字节确认相同后去除旧副本，共 117 个文件 |

研究数据与 `work` 继续排除在 Git 之外；辅助验证目录也加入忽略规则。历史文档可随 master 追溯。没有新建分支，也没有保留旧路径软链接。

## 路径更新

主要数据库中 76 条、独立测试库中 1 条运行配置的 `config.data_dir` 改为新位置；研究判断、任务状态、账本、原始响应和冻结提示词均保留。另有一条本就使用相对路径的运行配置无需改变。

主仓库中 62 个现有辅助脚本的数据路径已更新，并完成语法检查。当前操作文档和档案入口已更新。历史日志、原始响应和旧文档中的路径仍表示当时位置，不批量改写历史证据。

在主仓库中使用已有记录时，显式传入数据目录：

```bash
cd /home/g203/zhanghaonan/adversarial-research-copilot
.venv/bin/arc --data-dir .arc-validation resume mcp-upgrade-20260912.discover
```

上述示例针对已完成任务，只读取状态并重新生成报告，不重新执行研究。对未完成任务使用 `resume` 则会按其状态继续执行，可能产生费用。主档案不更改新项目默认的 `.arc` 数据目录。

## 实际验证

- 迁移前没有进程打开旧档案，也没有系统服务、用户服务或定时任务依赖旧路径。
- 主档案有 77 次运行、299 个任务、4,200 条调用记录；主要原始文件 25,366 份，独立测试档案另有 64 份。移动前后文件数、字节总数、数据库表计数、账本逐行内容和运行状态一致。
- 两个数据库的 SQLite 结构检查通过；1,248 个数据库所引用的正文、任务响应、提示词和环境快照文件均可在新位置找到。
- 新位置没有遗留的旧绝对运行路径。通过 ARC 正式 CLI 成功读取最近三张卡的已完成运行并重新生成报告，仍为 `COMPLETED`，费用仍为 4.244226–4.244256 元、75 条本轮计费记录，没有新增调用。
- 旧目录已不存在，唯一 Git 工作区与分支均为主仓库的 master。没有进行文件哈希或 SHA256 校验。

私有操作清单与验证记录位于 `work/consolidation-20260912/`：`plan.json`、`result.json`、`updated_paths.json`、`readback.json`。其中记录了逐项位置变更与数据库路径更新，方便定位问题。
