# Discover First：恢复两例开发任务

用户在首轮交付后明确要求使用现有不足100元余额，把暂停的两例跑完；这项新授权替代本次文档之前的20元/40元及一次调整停止限制，不创建新的样例，不运行模型对照、研究实验或付费develop/run。

恢复前父账本剩余84.632470元（已扣旧费用及15.912元未知预留），父上限285.562384元不增加。两个原子账户上限均放宽为100元，实际请求仍受同一个父账本的剩余额度约束，不能分别花两份100元。旧费用及未知预留不清零。

保留首例已接受的SURVEY、SKETCH、TRIAGE和待预研seed；从原CHECK稿创建显式纠正任务版本。第二例从原SURVEY及已完成的工具材料创建显式任务版本。来源对应由模型根据真实目录选择，代码不猜测自动补证。

首例冻结资源中的discover/repair.md已登记一次开发修订，加入明确JSON要求。旧目录、旧task快照及修订前后正文均保留在该run的operator-recovery审计中；只影响后续新任务的模板，原拒绝记录不改为成功。此为用户授权的开发恢复，正式服务中CodeX仍不在运行环路。

## 第一例已完成

`discover-first-20260909.sample1` 已以 `discover_draws_finished_human_selection` 完成，5次构思、5份IdeaNote：3 discuss、1 lead、1 drop。原失败CHECK保持UNKNOWN，新 `idea1.check.protocol_retry1` 接受并接回原流程；第二个CHECK的字段拼写错误在任务内一次JSON纠正后接受，没有重跑调查。旧UNKNOWN费用仍单独保留。

| 候选 | ARC预研建议 | 当前判断要点 |
| --- | --- | --- |
| 三种必要性/求助标记的分歧 | discuss | 修正When2Tool标签含义，改用开放模型和较小原型入口 |
| 以任务收益选择提问内容 | lead | 找到已覆盖目标函数的近邻，剩余贡献需证明不是普通多模态迁移 |
| 获取过程的停止信号探针 | discuss | 发现文本域已有停止/充分性表征近邻，将区别收窄到多模态迭代获取 |
| 异质提问成本与不可逆失败 | discuss | 给出已有策略与数据入口，但严格不可逆条件的实现与解释仍有限制 |
| 从真实日志反推用户效用 | drop | CHECK认为指定公开数据缺少反馈、身份与外生变异，无法支持原识别路径；未为保住想法改成采集新数据 |

这些是ARC的预研建议，不是独立科学验收。开发阅读还看到两类需要人工谨慎处理的表述：线性探针接近随机不能证明信号不存在；单一训练设置失败不能证明一类方案普遍不可行。第1/3张都使用内部表征探针，区别是否足以独立立题也仍需判断。报告保留实际输出，未把这些局限偷偷写成已消除。

第一例截至完成已结算上界8.994797元，相对首轮3.474208元新增5.520589元；15.912元未知预留不计作已花费。

## 第二例已完成

原SURVEY来源ID错误已通过显式新任务版本纠正，保留原34次成功工具材料。第二例也以discover_draws_finished_human_selection完成，5次构思、5份IdeaNote，全部为discuss。最后一张field_updates错层/类型错误通过本任务一次JSON纠正后接受。

| 候选 | ARC预研后的问题与限制 |
| --- | --- |
| 条件成功图 | 统计维护机制已有近邻，收窄为环境变化后的选择性更新；稀疏后验可能不如失败后重写 |
| 因子差分基准 | 修正单轴干预的新颖性及GOAT-Bench设定；失效标签可靠性仍未验证 |
| 局部模拟器预检 | 已有模拟器验证近邻，剩余问题是局部修正成本与成败校准；核心成本优势尚无证据 |
| 技能描述索引 | 已有系统会同时更新描述与代码，剩余问题是环境驱动的检索失配是否真的存在 |
| 角色-实体绑定 | 原Voyager动机被撤回，转向缓存身份/位置的技能；须胜过每次执行重新查询这个简单基线 |


开发阅读仍发现需要人判断的推断：仅凭摘要不能断言技能维护近邻完全没有变化适应；静态代码/描述中的因子token加有限次配对执行，也不能自动保证穷尽、可靠的条目失效ground truth。第二张note已将其列为未实验验证的设计，但部分正文使用“可生成”“无需人工标注”等较强措辞。报告保留这种原文与限制并存的状态，不把流程完成当作科学论证已完备。

五张均被标为discuss只代表模型建议讨论，不能据此宣布科研质量通过。报告仍有“完全空白”“没有任何”等超出检索覆盖范围的表述；“能迁移到新世界”也不证明一切布局/外观变化都不会使技能失效。这些是阅读时应保留的限制。首轮失败与费用不改写。

## 续跑实际暴露的缓存空转

第二例完成前两张预研后，survey.refresh在补查中反复成功读取同一缓存，改变limit但没有新增内容。旧保护只识别重复EOF，未覆盖成功读旧范围。开发侧通过暂时关闭该子账户的下一请求准入让当前调用正常结算；停止时第二例累计11.314380元、无在途调用或新增UNKNOWN，所有旧读记录保留。这个PAUSED_BUDGET是人为安全收束措施，不是余额不足。恢复时还原原100元子上限，继续受同一父余额约束。

修复目标是连续没有新增来源/内容范围时先提醒一次基于已有材料完成当前任务；仍继续无进展读取才暂停。它不限制正常新资料、分页或科学判断，也不以工具成功次数宣称调研充分。

`2378dfd`补齐提醒与恢复，124项相关回归通过。真实旧trace只读回放发现177次工具记录中末尾129次没有新增正文；原EOF判断为false。注册新Markdown后原任务直接恢复，首次模型响应完成共享简报并接受，没有新增工具读取，流程进入idea3。原任务仍是同一个survey.refresh，不伪造新成功副本；这证明此现场能收束，不证明所有模型循环都已消除。

## 最终交付与费用

两例执行均COMPLETED，各5/5次构思、5份预研记录，无pending，未创建完整研究卡，未执行付费develop/run或研究实验。第一例3 discuss、1 lead、1 drop；第二例5 discuss。

续跑新增已结算上界20.371510元，两例累计25.362702元；原未知调用1次及15.912000元预留独立保留。父账本剩余64.260960元，本轮未扩大父上限，未追加新题目。费用与任务/请求/工具次数见[续跑结果](DISCOVER_FIRST_CONTINUATION_RESULTS.json)。

报告展示修正bf7fb41：当前预研判断和实质调整先显示，原seed明确标为预研前构思；共享概览后置并标为阶段性资料，不再在首页无标记重复已被修正的原句。38项报告/提示词相关回归通过，原模型对象及历史稿不变。软件包包含这项展示修正、2378dfd缓存收束修复及0e8a8d8拒绝请求恢复修复。
