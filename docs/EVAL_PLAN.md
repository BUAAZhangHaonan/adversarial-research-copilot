# Frozen validation plan — 2026-09-07, before paid validation

## Budget and order

All paid validation uses the single persistent parent `arc-vnext-validation-20260907`
in the isolated g203 worktree's `.arc-validation/arc.sqlite`. Its authorization is CNY 100.
Each discover/develop/run child is at most CNY 20 including MCP calls, retries and repair.
The parent is reused, never recreated on resume. Baseline/evaluator calls use children of
this same parent. No research training or proposed scientific experiments will run.

Order: L1 deterministic contracts → read-only service inventory/cost audit → L2 real
Flash/Pro max/stream/tool protocol and three entry points → L3 natural same-card chain
→ L4 frozen-material comparison. Protect L2/L3 funds before spending on L4.
Unknown MCP upper costs block those paid calls. Actual usage estimates and external
upper bounds are reported separately; model approval is never a successful experiment.

## Fixed topics

1. Small-instance segmentation: when crowded boundaries make an instance metric hide
   merge/split errors. Relevant to the user's vision work; development case.
2. Synthetic-to-real visual evidence: disentangling material/lighting changes from
   geometry changes in a fixed scene. Relevant to rendering pipelines; development case.
3. Multimodal conflict understanding: separating modality reliability from label
   ambiguity with controlled observations. Related to the user's research; **holdout**.

Do not substitute all cases with agent-memory questions. Do not tune prompts on the holdout.

## L1

Run contracts B01–B18 and section 11 matrix. Test final persisted state, full transmitted
inputs, valid accounting and prohibited calls, not merely a mock argument. Package/install
the CLI, then invoke outside the repository. Record baseline's 87 passing tests as historical
engineering context, not proof of the replacement implementation.

## L2 and L3

Use `arc test-e2e --allow-stage-transition` only for this software validation. Normal commands
must stop after their own stage. Capture a genuine additional source read/search and link
it to an issue and changed/unchanged judgment. Demonstrate a non-destructive resume using
persisted successful tasks; do not deliberately burn CNY 20. If discover has no main card,
preserve that result and use an explicitly labelled imported development fixture to test
other entry points. That is L2 only. L3 requires a naturally discovered, explicitly selected
card with unbroken ID/version lineage. Rejection/lead/scope-change remains a valid outcome.

## L4

Freeze one evidence snapshot per topic. Compare direct Pro generation/judgment and ARC
using the same materials, model versions, max effort and comparable actual budgets.
Keep old historical outputs as context only. Shuffle candidate order with recorded seed
and remove system names, roles and self-ratings before isolated evaluator invocation.
Evaluate knowledge increment, closest-work coverage, source correctness, test identifiability,
resources, unjustified combinations, distinctness and cross-stage fidelity. Record false
rejections, unresolved findings and costs. Inspect decisive source claims manually.

The automatic evaluator is an initial audit, not expert ground truth or proof of improvement.
Report inconclusive comparisons honestly. Prompt changes require recorded failing output,
diff and development-case recheck; no keyword blacklists or score-based stopping fixes.
